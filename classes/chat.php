<?phpnamespace local_learningcompanions;class chat {    protected $chatid;    public function __construct($chatid) {        $this->chatid = $chatid;        $this->filestorage = get_file_storage();    }    public function get_comments() {        global $DB;        $comments = $DB->get_records('lc_chat_comment', array('chatid' => $this->chatid), 'timecreated');        foreach($comments as $comment) {            $comment->attachments = $this->get_attachments_of_comment($comment->id);        }        return $comments;    }    public function get_attachments_of_comment($commentid) {    }}