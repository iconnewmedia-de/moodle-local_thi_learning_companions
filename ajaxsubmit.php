<?phprequire_once __DIR__ . "/../../config.php";require_once __DIR__ . "/classes/post_form.php";require_once __DIR__ . "/locallib.php";global $DB;$customdata = [  // ICTODO: fill with data if necessary];$form = new \blocks\learningcompanions_chat\post_form(null, $customdata);if ($data = $form->get_data()) {    // ICTODO: save the form data    $status = \block\learningcompanions_chat\handle_submission($data, $form);    if ($status["success"]) {        http_response_code(200);    } else {        http_response_code(400);        echo json_encode($status);    }} else {    http_response_code(400);}die();