import React from "react";import Post from "./Post";export default function Postlist({posts}) {    return (        <div id="learningcompanions_chat-postlist">            {posts.map(post => (                <Post author={post.author} key={post.id} id={post.id} datetime={post.datetime} message={post.message} attachments={post.attachments} />            ))}        </div>            );};